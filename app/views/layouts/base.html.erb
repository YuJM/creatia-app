<!DOCTYPE html>
<html>
  <head>
    <%= render 'layouts/head' %>
  </head>

  <body class="<%= content_for(:body_class) || 'min-h-screen' %>">
    <%= render 'layouts/flash_messages' %>
    
    <%= yield %>
  </body>
</html>