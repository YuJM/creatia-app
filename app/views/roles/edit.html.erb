<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
  <div class="mb-8">
    <%= link_to organization_roles_path(@organization),
                class: "inline-flex items-center text-sm text-gray-500 hover:text-gray-700" do %>
      <svg class="mr-1 h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
      </svg>
      역할 목록으로 돌아가기
    <% end %>
  </div>

  <div class="bg-white shadow sm:rounded-lg">
    <div class="px-4 py-5 sm:p-6">
      <h3 class="text-lg font-medium leading-6 text-gray-900">역할 수정</h3>
      <div class="mt-2 max-w-xl text-sm text-gray-500">
        <p>역할의 정보와 권한을 수정합니다.</p>
      </div>
      <div class="mt-5">
        <%= render 'form', role: @role, organization: @organization, available_permissions: @available_permissions %>
      </div>
    </div>
  </div>
</div>