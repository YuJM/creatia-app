{
	auto_https off
}

# 모든 서브도메인을 포함한 와일드카드 설정
http://*.creatia.local, http://creatia.local {
	reverse_proxy localhost:3000 {
		header_up Host {http.request.host}
		header_up X-Real-IP {http.request.remote.host}
	}
}
